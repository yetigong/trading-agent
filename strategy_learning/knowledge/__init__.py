"""Knowledge base ownership (Phase 4.5.3).

Today KB lives in ``trading_agent.agents.knowledge``. This package will own
writes; ``trading_agent`` will keep a read-only view for prompt soft context.
"""
